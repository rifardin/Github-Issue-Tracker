

- Create a readme file and answer this question on your own. Don’t copy-paste from Google or any AI chatbot. 


    - 1️⃣ What is the difference between var, let, and const?

    Ans :    var can both reassign the value and redeclare the variable self within the same name.

              let can reassign the value, but cannot redeclare it in the same name.

              const cannot reassign a new value or redeclare the variable

    - 2️⃣ What is the spread operator (...)?
    
             spread operator can use the spread operator to combine arrays or pass array elements individual arguments to a function.

    - 3️⃣ What is the difference between map(), filter(), and forEach()?

              map =       create a new array by appley a function to every element of the original array
              filter =     create a new array contain only the elements that pass a specific condition
              forEach =  execute a function for each element in the array
                 
    - 4️⃣ What is an arrow function?

    arrow function is a modern feature of ES6, instead of a normal function, a arrow-like symbol => is use here

    - 5️⃣ What are template literals?






